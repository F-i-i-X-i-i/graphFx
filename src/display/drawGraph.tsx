import React from "react";
import * as Interface from "../interface/graphFx"

const drawGraph: React.FC<{graph: Interface.GraphFx}> = ({graph}) => {
    //TODO реализация useState, svg - очистка, закидываем ноды, connection, ref  - гуглить
    return <p>123</p>
}
